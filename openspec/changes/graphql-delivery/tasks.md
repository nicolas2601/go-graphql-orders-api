# Tasks

## 1. Schema y gqlgen
- [x] 1.1 Schema final + gqlgen.yml + generate

## 2. Resolvers y mapeo
- [x] 2.1 Resolvers (delegan) + UserUseCase + mapeo dominio->modelo + mapeo de errores

## 3. Auth y server
- [x] 3.1 authctx + middleware Bearer + handler con anti-DoS y fail-safe
